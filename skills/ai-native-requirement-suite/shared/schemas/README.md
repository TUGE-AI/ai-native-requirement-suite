# Shared Schemas

status: `skeleton_v0.15_fix`

本目录用于承载 AI-native Requirement Suite 的共享 schema。V0.15-fix 只建立最小结构，不定义完整 schema。

后续 V0.19 `ai-ready-spec hardening` 必须在这里沉淀 requirement asset、Feature、Story、Rule、GWT、Gate、Question 等结构契约。
